import numpy as np
import cv2
import pupil_apriltags
import argparse
import VisionUtills
import SystemConsts

# Set up video feed from the camera

# Set up apriltag detector

# Start looping over frames.
    # Pull new frame from the camera

    # Convert to grayscale

    # Detect apriltags in the image

    # Estimate the possible poses to each apriltag

    